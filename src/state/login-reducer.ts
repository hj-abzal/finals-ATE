const initialState = {
    email: "",
    password: ""
}

export const loginReducer = (state: any, action: any) => {
    switch (action.type) {
        case "LOGIN": {
            const StateCopy = {...state}
            return
        }
        case "PASSWORD": {
            return
        }
    }
}

